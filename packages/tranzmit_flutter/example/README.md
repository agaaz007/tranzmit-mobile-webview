# Tranzmit Flutter Example

This example initializes `TranzmitProvider` and opens the `upgrade_pro` placement when the SDK becomes ready.

If platform folders are not present yet, generate them with Flutter:

```bash
flutter create .
flutter pub get
flutter run
```

Then update `apiBaseUrl` in `lib/main.dart` with the customer's Tranzmit API URL.
