name: tranzmit_flutter_example
description: Example app for the Tranzmit Flutter SDK.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  tranzmit_flutter:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  http: ^1.6.0

flutter:
  uses-material-design: true
