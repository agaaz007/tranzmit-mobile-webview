name: tranzmit_flutter
description: Client Flutter SDK for Tranzmit server-driven paywalls, Statsig-backed assignment, and conversion tracking.
version: 0.1.0
homepage: https://tranzmit.com
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  uuid: ^4.5.1
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
